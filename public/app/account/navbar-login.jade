.navbar-right(ng-controller="psNavBarLoginCtrl")
    form.navbar-form
        .form-group
            input.form-control(placeholder="Email", ng-model="username")
        .form-group
            input.form-control(type="password", placeholder="Password", ng-model="password")
        button.btn.btn-primary(ng-click="signin(usrname, password)") Sign In
        //button.btn.btn-primary(ng-click="signin(usrname, password)") **** CREATE SIGN UP BUTTON HERE!! ****